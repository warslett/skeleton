{% extends "_base.html.twig" %}

{% block title %}{{ title ?? 'Form'}} &raquo; {{ parent() }}{% endblock %}

{% block header_h1 %}{{ title ?? 'Form'}}{% endblock %}

{% block body_content %}
    <div class="row">
        <div class="col-lg-8 col-xl-6">
            <div class="card card-small mb-4">
                <div class="card-body">
                    {% block form %}
                        {{ form(form) }}
                    {% endblock %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
