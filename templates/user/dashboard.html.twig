{% extends "_base.html.twig" %}

{% block title %}Dashboard &raquo; {{ parent() }}{% endblock %}

{% block header_h1 %}Dashboard{% endblock %}

{% block body_content %}
    <div class="row">
        <div class="col-lg-6">
            <div class="card mb-4">
                <div class="card-body">
                    Logged in as <strong>{{ app.user.email }}</strong>. <a href="{{ path('user_logout') }}">Log out</a>.
                </div>
            </div>
        </div>
    </div>
{% endblock %}
